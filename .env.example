# EEF Manager Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# SERVER CONFIGURATION
# ============================================
# Port for local development (not needed for Vercel)
PORT=3000

# Environment mode
# Local: development
# Vercel: production
NODE_ENV=development

# Frontend URL for CORS (only needed for local development)
# Vercel automatically handles same-origin requests
FRONTEND_URL=http://localhost:8000

# ============================================
# EMAIL CONFIGURATION (SMTP)
# ============================================
# Required for both local development AND Vercel deployment

# SMTP server hostname
# Gmail: smtp.gmail.com
# Outlook: smtp-mail.outlook.com
SMTP_HOST=smtp.gmail.com

# SMTP port
# Gmail with SSL: 465
# Gmail with TLS: 587
SMTP_PORT=465

# Your email address
SMTP_USERNAME=your-email@gmail.com

# Gmail App Password (NOT your regular password)
# Generate at: https://myaccount.google.com/apppasswords
# For other providers, use your email password or app-specific password
SMTP_PASSWORD=your-app-password-here

# Whether to use SSL (true for port 465, false for port 587)
SMTP_SECURE=true

# Email address that appears in "From" field
# Usually the same as SMTP_USERNAME
FROM_EMAIL=your-email@gmail.com

# ============================================
# VERCEL DEPLOYMENT
# ============================================
# When deploying to Vercel, set these environment variables:
#
# Required variables:
# - SMTP_HOST
# - SMTP_PORT
# - SMTP_USERNAME
# - SMTP_PASSWORD
# - FROM_EMAIL
# - NODE_ENV (set to "production")
#
# NOT needed on Vercel:
# - PORT (Vercel manages this automatically)
# - FRONTEND_URL (same-origin requests are auto-allowed)
#
# How to set on Vercel:
# 1. Via Dashboard: Project Settings → Environment Variables
# 2. Via CLI: vercel env add VARIABLE_NAME
